<!DOCTYPE html>
<html>
<head>
  <title>Treebook</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="navbar-header">
  	<div class="container">
    <a class="navbar-brand" href="#">Treebook</a>
    <ul class="nav nav-tabs">
  <li><%= link_to "All Statuses", feed_path %></li>

  <% if user_signed_in? %>
      <li class="nav pull-right"><%= link_to current_user, edit_user_registration_path %></li>
      <li class="nav pull-right"><%= link_to "Log Out", logout_path %></li>
      <% else %>
        <li class="nav pull-right"><%= link_to "Register", register_path %></li>
        <li class="nav pull-right"><%= link_to "Log in", login_path %></li>

  <% end %>
</ul>


  </div>
  </div>
</nav>
<div class="container">
    <% flash.each do |type, message| %>

    <div class="alert <%= flash_class type %>">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">x</button>
      <%= message %>
    <% end %>
</div>
</div>
<div class="container">
  <%= yield %>
</div>

</body>
</html>
